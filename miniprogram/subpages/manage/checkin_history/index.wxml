<view class="signin-history-page">
  <van-notify id="van-notify" />

  <!-- 加载中 -->
  <view wx:if="{{ loading && checkinList.length === 0 }}" class="loading-container">
    <van-loading size="24px">加载中...</van-loading>
  </view>
  
  <!-- 签到记录列表 -->
  <van-cell-group wx:if="{{ !loading || checkinList.length > 0 }}">
    <van-cell 
        wx:for="{{ checkinList }}" 
        wx:key="id" 
        title="{{ item.formattedTime }}"
        label="{{ item.checkinAddress || '未知位置' }}"
    >
        <van-icon 
            slot="right-icon" 
            name="success" 
            color="green" 
            class="custom-icon" 
      />
    </van-cell>
  </van-cell-group>
  
  <!-- 加载状态提示 -->
  <view wx:if="{{ checkinList.length > 0 }}" class="loading-view">
    <van-loading wx:if="{{ loading }}" size="24px">加载中...</van-loading>
    <van-divider contentPosition="center" wx:elif="{{ !pageInfo.hasMore }}">没有更多了</van-divider>
  </view>
  
  <!-- 空状态 -->
  <van-empty 
        wx:if="{{ !loading && checkinList.length === 0 }}"
        image="search" 
        description="暂无签到记录"
      />
</view>